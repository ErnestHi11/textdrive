parameters:
- name: myStringName
  type: string
  default: a string value

trigger:
- master

pool:
  vmImage: ubuntu-latest

steps:

- script: echo building $(Build.BuildNumber) with ${{ parameters.myStringName }}