parameters:
- name: myStringName
  type: string
  default: a string value
variables:
- name: one
  value: initialValue

trigger:
- master

pool:
  vmImage: ubuntu-latest

steps:

- script: |
  echo building $(Build.BuildNumber) with ${{ parameters.myStringName }}
  echo ${{ variables.one }}